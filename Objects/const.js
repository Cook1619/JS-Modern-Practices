const isRaining = true
// can't re-assign a variable
// isRaining = false

console.log(isRaining);

const person = {
    age: 13
}
// can't do this
// person = {}
//this is just changing an objects property, this is valid
person.age =  16
console.log(person.age)
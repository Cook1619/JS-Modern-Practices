//Notes
//Can't define a variable more then once, you can reassign once initialized
//Variables can't use JS reserved keywords
//variables have to start with A-Z, a-z, _ or $ nothing else is valid
let petName = 'Kali';
petName = 'Spot';

console.log(petName);
